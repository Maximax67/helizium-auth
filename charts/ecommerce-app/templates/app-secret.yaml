apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  labels:
    {{- include "ecommerce-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.secret.databaseUrl | quote }}
  EMAIL_PASSWORD: {{ .Values.secret.emailPassword | quote }}
